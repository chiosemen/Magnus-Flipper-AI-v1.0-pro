# Hoist packages to root node_modules to fix Next.js 14 standalone output
# symlink resolution issues with pnpm
shamefully-hoist=true

# Force pnpm@8 for Vercel stability (compatible with Next.js 16 + Turbopack)
package-manager=pnpm@8
