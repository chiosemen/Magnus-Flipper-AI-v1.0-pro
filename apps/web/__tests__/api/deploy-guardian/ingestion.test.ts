import { describe, it } from 'vitest';

// TODO: Move to deploy-guardian integration suite (depends on removed routes + Prisma core).

describe.skip('Deploy-Guardian ingestion', () => {
  it('skipped: deploy-guardian routes removed or relocated', () => {});
});
