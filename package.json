{
  "name": "magnus-flipper-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "prebuild": "pnpm --filter '@magnus-flipper-ai/*' build",
    "build": "turbo run build --filter=web",
    "build:packages": "pnpm --filter '@magnus-flipper-ai/*' build",
    "build:web": "pnpm build:packages && pnpm --filter web build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "generate": "prisma generate --schema=packages/core/prisma/schema.prisma",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "dev:mobile": "pnpm --filter mobile dev",
    "dev:worker": "pnpm --filter magnus-worker dev",
    "start:web": "pnpm --filter web start",
    "start:api": "pnpm --filter api start",
    "start:worker": "pnpm --filter magnus-worker start",
    "spec:check": "ts-node scripts/validate-imports.ts",
    "deploy:check": "bash scripts/deploy-check.sh",
    "release:check": "pnpm lint && pnpm test && pnpm build",
    "secrets:gate": "node scripts/deploy/secrets-integrity-gate.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "autoprefixer": "^10.4.18",
    "postcss": "^8.4.35",
    "prisma": "^7.0.1",
    "tailwindcss": "^3.4.15",
    "ts-node-dev": "^2.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@prisma/client": "^7.0.1",
    "bullmq": "^5",
    "framer-motion": "^11.0.0",
    "libsodium-wrappers": "^0.7.15",
    "winston": "^3",
    "zod": "^3"
  }
}
